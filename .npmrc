# XINVORA .npmrc
# Approve install scripts for known native packages.
# These packages require node-gyp/postinstall scripts to build native binaries.
# Suppresses "npm warn allow-scripts" in Vercel build logs.
allow-scripts=true
